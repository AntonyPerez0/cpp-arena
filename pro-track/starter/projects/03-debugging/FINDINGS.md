# Findings

For each bug, write one bullet: what the tool reported, where the bug was, and how you fixed it.
Example: "- ASan heap-buffer-overflow in Foo::bar (foo.cpp:42): loop used <= instead of <; fixed the bound."

