id: calculator
title: Command-line calculator
lang: c
level: Beginner
after: c-loops
summary: Read expressions like `3 + 4`, handle every operator, refuse division by zero, and keep going until the user types q.
seed: |
  #include <stdio.h>

  int main(void) {
      double a, b;
      // read two numbers and print their sum

      return 0;
  }
milestones:
  - title: Add two numbers
    text: |
      Read two numbers from input and print their sum with two decimals.

      Input:
      ```
      3 4.5
      ```
      Output:
      ```
      7.50
      ```

      Use `scanf("%lf %lf", &a, &b)` to read two doubles, then `printf("%.2f\n", a + b)`.
    solution: |
      #include <stdio.h>

      int main(void) {
          double a, b;
          scanf("%lf %lf", &a, &b);
          printf("%.2f\n", a + b);
          return 0;
      }
    tests:
      - stdin: "3 4.5\n"
      - stdin: "-2 2\n"
      - stdin: "0.1 0.2\n"
        hidden: true
    hints:
      - "`%lf` reads a double with scanf. Don't forget the `&` before each variable."
      - "`%.2f` prints a number with exactly two digits after the decimal point."
  - title: Pick the operator
    text: |
      Now the input includes an operator between the numbers: `+`, `-`, `*` or `/`.

      ```
      6 * 7
      ```
      prints `42.00`.

      Read the operator with `%c`. Put a space before it in the format (`" %c"`) so scanf skips the space after the first number.
    solution: |
      #include <stdio.h>

      int main(void) {
          double a, b;
          char op;
          scanf("%lf %c %lf", &a, &op, &b);
          double r = 0;
          switch (op) {
              case '+': r = a + b; break;
              case '-': r = a - b; break;
              case '*': r = a * b; break;
              case '/': r = a / b; break;
          }
          printf("%.2f\n", r);
          return 0;
      }
    tests:
      - stdin: "6 * 7\n"
      - stdin: "10 - 12.5\n"
      - stdin: "9 / 4\n"
      - stdin: "1.5 + 1.5\n"
        hidden: true
    hints:
      - "`scanf(\"%lf %c %lf\", &a, &op, &b)` reads number, operator, number."
      - A `switch (op)` with `case '+':` etc. keeps it tidy. Remember `break;` after each case.
  - title: Guard against bad input
    text: |
      Two things can go wrong:

      - Dividing by zero should print `error: division by zero`
      - An unknown operator like `%` should print `error: unknown operator %`

      Everything else works as before.
    solution: |
      #include <stdio.h>

      int main(void) {
          double a, b;
          char op;
          scanf("%lf %c %lf", &a, &op, &b);
          double r = 0;
          switch (op) {
              case '+': r = a + b; break;
              case '-': r = a - b; break;
              case '*': r = a * b; break;
              case '/':
                  if (b == 0) {
                      printf("error: division by zero\n");
                      return 0;
                  }
                  r = a / b;
                  break;
              default:
                  printf("error: unknown operator %c\n", op);
                  return 0;
          }
          printf("%.2f\n", r);
          return 0;
      }
    tests:
      - stdin: "8 / 0\n"
      - stdin: "8 % 3\n"
      - stdin: "8 / 2\n"
      - stdin: "2 ^ 2\n"
        hidden: true
    hints:
      - Check `b == 0` inside the `/` case before dividing.
      - The `default:` case of a switch runs when no other case matches.
  - title: Keep calculating until q
    text: |
      Turn it into a loop. Keep reading expressions and printing results until the input line is just `q`.

      ```
      1 + 1
      5 / 0
      2 * 3
      q
      ```
      prints
      ```
      2.00
      error: division by zero
      6.00
      ```

      Tip: `scanf` returns how many values it read. `scanf("%lf %c %lf", &a, &op, &b)` returns 3 for a full expression, but when it meets `q` it can't read a number, so it returns 0. Loop while it returns 3 and the `q` ends the loop for you.
    solution: |
      #include <stdio.h>

      int main(void) {
          double a, b;
          char op;
          while (scanf("%lf %c %lf", &a, &op, &b) == 3) {
              switch (op) {
                  case '+': printf("%.2f\n", a + b); break;
                  case '-': printf("%.2f\n", a - b); break;
                  case '*': printf("%.2f\n", a * b); break;
                  case '/':
                      if (b == 0) printf("error: division by zero\n");
                      else printf("%.2f\n", a / b);
                      break;
                  default: printf("error: unknown operator %c\n", op);
              }
          }
          return 0;
      }
    tests:
      - stdin: "1 + 1\n5 / 0\n2 * 3\nq\n"
      - stdin: "7 - 10\n3 x 3\nq\n"
      - stdin: "2.5 * 4\n100 / 8\nq\n"
        hidden: true
    hints:
      - "Put the scanf call in the loop condition: `while (scanf(\"%lf %c %lf\", &a, &op, &b) == 3)`."
      - The division-by-zero and unknown-operator cases now print their error and carry on to the next line, so replace their `return 0` with `break` (which leaves the switch, not the loop).
